This program will display a rotating cube making use of the rotation matrix

commands:
    $ venv\bin\activate                 #activate venv environment (unix systems)
    $ venv\Scripts\activate             #activate venv environment (windows)
    $ path\to\venv\bin\activate
    $ deactivate                        #turn off venv 
    $ pip install -r requirements.txt   #install dependancies
    $ python app                        #run application

    space  - pause
    f      - fill